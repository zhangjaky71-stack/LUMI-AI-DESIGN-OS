[project]
name = "lumi-event-contract"
version = "0.1.0"
description = "Language-neutral LUMI event envelope reference runtime"
requires-python = "==3.12.*"
dependencies = []

[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"
