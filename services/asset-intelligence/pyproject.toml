[project]
name = "lumi-asset-intelligence"
version = "1.0.0"
description = "LUMI Asset Intelligence V1 semantic indexing and retrieval runtime"
requires-python = "==3.12.*"
dependencies = []

[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"
