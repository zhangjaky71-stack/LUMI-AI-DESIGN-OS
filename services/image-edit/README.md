# LUMI Image Edit

NODE-47 provider-neutral structural-first and protected local image-edit runtime.

The package intentionally has no provider SDK dependency. Design IR edits remain model-free;
pixel edits use ports for source authorization, Model Gateway, durable output materialization,
postflight validation, Artifact append, Canvas replacement, audit, and cost projection.
