[project]
name = "lumi-asset-storage"
version = "0.0.0"
requires-python = "==3.12.*"
dependencies = []

[project.optional-dependencies]
s3 = ["boto3>=1.42,<2"]

[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"
