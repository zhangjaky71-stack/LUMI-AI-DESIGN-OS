[project]
name = "lumi-sandbox-runtime"
version = "0.0.0"
requires-python = "==3.12.*"
dependencies = [
  "pydantic==2.13.4",
]

[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"
