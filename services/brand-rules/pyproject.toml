[project]
name = "lumi-brand-rules"
version = "1.0.0"
description = "LUMI Brand Rules Engine V1 reference runtime"
requires-python = "==3.12.*"
dependencies = []

[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"
