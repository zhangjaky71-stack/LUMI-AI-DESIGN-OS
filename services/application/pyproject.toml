[project]
name = "lumi-application"
version = "0.1.0"
description = "LUMI application-service transaction and use-case boundary"
requires-python = "==3.12.*"
dependencies = []

[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"
