[project]
name = "lumi-design-ir"
version = "1.0.0"
description = "LUMI Design IR V1 reference runtime and conformance helpers"
requires-python = "==3.12.*"
dependencies = []

[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"
