[project]
name = "lumi-auth"
version = "0.1.0"
description = "LUMI authentication policy, tenant guard and opaque-token contracts"
requires-python = "==3.12.*"
dependencies = []

[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"
