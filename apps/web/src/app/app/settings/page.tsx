import { ShellSection } from "@/components/app-shell/shell-section";

export default function SettingsPage() {
  return (
    <ShellSection
      eyebrow="PREFERENCES"
      title="设置"
      description="管理工作区偏好、通知与产品体验设置。"
    />
  );
}
