apiVersion: v1
kind: Service
metadata:
  name: api
spec:
  type: ClusterIP
  selector: {app.kubernetes.io/name: api}
  ports: [{name: http, port: 8000, targetPort: http}]
---
apiVersion: v1
kind: Service
metadata:
  name: agent-runtime
spec:
  type: ClusterIP
  selector: {app.kubernetes.io/name: agent-runtime}
  ports: [{name: http, port: 8080, targetPort: http}]
---
apiVersion: v1
kind: Service
metadata:
  name: model-gateway
spec:
  type: ClusterIP
  selector: {app.kubernetes.io/name: model-gateway}
  ports: [{name: http, port: 8080, targetPort: http}]
---
apiVersion: v1
kind: Service
metadata:
  name: tool-gateway
spec:
  type: ClusterIP
  selector: {app.kubernetes.io/name: tool-gateway}
  ports: [{name: http, port: 8080, targetPort: http}]
---
apiVersion: v1
kind: Service
metadata:
  name: sandbox-runtime
spec:
  type: ClusterIP
  selector: {app.kubernetes.io/name: sandbox-runtime}
  ports: [{name: http, port: 8080, targetPort: http}]
