# NODE-68 local recovery profile only.
# Password auth is mandatory for network connections; do not copy this broad CIDR
# policy into production. Production backup networks must be restricted by infra/IAM.
local   all             all                                     trust
host    all             all             0.0.0.0/0               scram-sha-256
host    all             all             ::/0                    scram-sha-256
host    replication     all             0.0.0.0/0               scram-sha-256
host    replication     all             ::/0                    scram-sha-256
