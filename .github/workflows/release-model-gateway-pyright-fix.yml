name: Release Model Gateway Pyright Fix (Disabled)

on:
  pull_request:
    branches:
      - node-73-final-acceptance-release
    paths:
      - ".github/workflows/release-model-gateway-pyright-fix.yml"

permissions:
  contents: read

concurrency:
  group: release-model-gateway-pyright-fix
  cancel-in-progress: true

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Superseded by release-model-gateway-pyright-fix-v2.yml"
